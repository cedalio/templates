type User {
  id: UUID!
  firstName: String!
  lastName: String!
  emails: [String!]
  age: Int!
  posts: [Post] @relation(type: REFERENCES)
}

type Post {
  id: UUID!
  slug: String!
  title: String!
  content: String
  comments: [String]
}